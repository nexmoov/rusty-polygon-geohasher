# This file is autogenerated by maturin v1.7.0
# To update, run
#
#    maturin generate-ci github
#
name: CI

on:
  push:
    branches:
      - main
      - master
    tags:
      - '*'
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  run_pre_commit:
    name: Run pre-commit hooks
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install Poetry
        run: pipx install poetry
      - uses: actions/setup-python@v5
        with:
          python-version: 3.12
          cache: "poetry"
      - uses: pre-commit/action@v3.0.1
  tests:
    runs-on: ubuntu-latest
    needs: [run_pre_commit]
    steps:
      - uses: actions/checkout@v4
      - name: Install Poetry
        run: pipx install poetry

      #- name: Install Poetry
      #  uses: snok/install-poetry@v1
      #  with:
      #    virtualenvs-create: true
      #    virtualenvs-in-project: true
      - uses: actions/setup-python@v5
        with:
          python-version: 3.12
          cache: "poetry"
      - name: Add Poetry to PATH
        run: |
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          pipx ensurepath

      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          args: --release --out dist
          sccache: 'true'
      - name: pytest
        run: |
          pipx ensurepath
          poetry self add poetry-plugin-export
          poetry export -f requirements.txt --with dev --output requirements.txt
          set -e
          python3 -m venv .venv
          source .venv/bin/activate
          pip install -r requirements.txt
          pip install rusty_polygon_geohasher --find-links dist --force-reinstall
          pip install pytest
          pytest

  linux:
    #needs: [tests]
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
            target: armv7
        duckdb_line: ["1.4"]
    env:
      DUCKDB_EXTENSION_NAME: geohash
    steps:
      - uses: actions/checkout@v4
        with: { submodules: true }
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
      - name: Install Poetry
        run: pipx install poetry
      - uses: actions/setup-python@v5
        with:
          python-version: 3.12
      # --- Build the DuckDB extension for this minor line ---
      - name: Build DuckDB extension
        shell: bash
        env:
          EXTENSION_NAME: polygon_geohasher
          TARGET_DUCKDB_VERSION: v${{ matrix.duckdb_line }}
        run: |
          set -eux
          cd duckdb_extension
          make configure
          mkdir -p build/release
          mkdir -p target/release
          make release
          mkdir -p rusty_polygon_geohasher/_duckdb/${{ matrix.duckdb_line }}/${{ matrix.platform.platdir }}
          ls -l build/release || true
          ls -l ./ || true
          cp build/release/polygon_geohasher.duckdb_extension \
             rusty_polygon_geohasher/_duckdb/${{ matrix.duckdb_line }}/${{ matrix.platform.platdir }}polygon_geohasher.duckdb_extension
          ls -l rusty_polygon_geohasher/_duckdb/${{ matrix.duckdb_line }}/${{ matrix.platform.platdir }}polygon_geohasher.duckdb_extension
      - name: Start interactive debugging session
        uses: mxschmitt/action-tmate@v3
      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist
          sccache: 'true'
          manylinux: auto
      - name: Inspect wheel contents
        shell: bash
        run: |
          python - <<'PY'
          import glob, zipfile
          whl = sorted(glob.glob('dist/*.whl'))[-1]
          print("Inspecting:", whl)
          with zipfile.ZipFile(whl) as z:
              for n in z.namelist():
                  #if "_duckdb/" in n:
                  print("  ", n)
          PY
      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-linux-${{ matrix.platform.target }}-duckdb-${{ matrix.duckdb_line }}
          path: dist

  release:
    name: Release
    runs-on: ubuntu-latest
    if: "startsWith(github.ref, 'refs/tags/')"
    needs: [linux]
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: wheels-macos-x86_64
          path: dist
      - uses: actions/download-artifact@v4
        with:
          name: wheels-macos-aarch64
          path: dist
      - uses: actions/download-artifact@v4
        with:
          name: wheels-linux-x86_64
          path: dist
      - uses: actions/download-artifact@v4
        with:
          name: wheels-linux-x86
          path: dist
      - uses: actions/download-artifact@v4
        with:
          name: wheels-linux-aarch64
          path: dist
      - uses: actions/download-artifact@v4
        with:
          name: wheels-linux-armv7
          path: dist
      - uses: actions/download-artifact@v4
        with:
          name: wheels-musllinux-x86_64
          path: dist
      - uses: actions/download-artifact@v4
        with:
          name: wheels-musllinux-x86
          path: dist
      - uses: actions/download-artifact@v4
        with:
          name: wheels-musllinux-aarch64
          path: dist
      - uses: actions/download-artifact@v4
        with:
          name: wheels-musllinux-armv7
          path: dist
      - uses: actions/download-artifact@v4
        with:
          name: wheels-windows-x86
          path: dist
      - uses: actions/download-artifact@v4
        with:
          name: wheels-windows-x64
          path: dist
      - uses: actions/download-artifact@v4
        with:
          name: wheels-sdist
          path: dist
      - name: Publish release distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
